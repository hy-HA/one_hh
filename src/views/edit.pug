extends base.pug

block content   
    h4 Change Title of Video 
    form(method="POST")
        input(name="title", placeholer="video Title", value=video.title)
        input(value="Save", type="submit")